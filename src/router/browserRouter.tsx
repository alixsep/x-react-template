import { createBrowserRouter } from 'react-router-dom'
import routerConfig from './config'

// Creates a browser router with the config in './config.tsx'.
const router = createBrowserRouter(routerConfig)

export default router
